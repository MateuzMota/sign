import { Doc } from "../../components";
import ActionButtons from "../ActionButtons";

const DocViewer = () => {
  return (
    <div>
      <ActionButtons />
      <Doc />
    </div>
  )
}

export default DocViewer;